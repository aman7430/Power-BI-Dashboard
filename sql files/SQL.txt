
select * from employees;



desc employees;

#selection and projection
select * from countries;

select country_id, country_name, region_id from countries;

select country_id, country_name from countries;



#operators (+, -, *, /)
select first_name, salary, salary + 1000 as "total salary"
from employees;

select first_name, salary, (12 * salary) + 200
from employees;

select first_name, salary, 12 * (salary + 200)
from employees;

select * from employees;


select 
first_name,
last_name,
salary,
commission_pct
from employees;


select first_name, salary, commission_pct, (12 * salary) + commission_pct
from employees;

select country_id, region_id
from countries;



select country_id || region_id as "Country and Region"
from countries;




select first_name ||' is a '|| job_id
as "employee details"
from employees;


#liminating duplicate rows

select job_id
from employees;

select distinct job_id
from employees;


select distinct job_id, department_id
from employees;



#where clause

select employee_id, first_name, job_id, department_id,
salary
from employees
WHERE department_id = 100;

select employee_id, first_name, job_id, department_id, salary
from employees
where last_name = 'King';



#comparison conditions
# =, >,>=, <, <=, <>

select first_name,salary
from employees
where salary >=5000;


select first_name,salary
from employees
where salary <>3000;

select first_name,salary
from employees
where salary !=3000;


#between and operator


select first_name,salary
from employees
where salary between 1000 and 3100;



select employee_id, first_name, salary, manager_Id, hire_date
from employees
where manager_id between 100 and 103;


select employee_id, first_name, salary, manager_Id
from employees
where manager_id in (100,101,102,103);


select employee_id, first_name, salary, manager_Id
from employees
where last_name in ('King','Ernst','Chen');



#wildcard charactorers

-like and wildcards
select first_name
from employees;



-like and wildcards
select first_name
from employees
where first_name like '__A%';

-like and wildcards
select first_name
from employees
where first_name like '%o';

-like and wildcards
select first_name
from employees
where first_name like '__a%';



