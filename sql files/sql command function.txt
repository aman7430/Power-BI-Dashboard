ALTER USER hr ACCOUNT UNLOCK;
ALTER USER hr IDENTIFIED BY test123;
SELECT * FROM employees; 
DESC employees; 
DESCRIBE employees





-- Selection and Projection 
SELECT * FROM countries 
SELECT country_id, country_name, region_id FROM 
countries 
SELECT country_id, country_name FROM countries 
-- Operators and Precedence 
-- / * + -
SELECT first_name, salary, salary + 500 
FROM employees; 
SELECT first_name, salary, 12 * salary + 200 
FROM employees; 
SELECT first_name, salary, ( 12 * salary ) + 200 
FROM employees; 
SELECT first_name, salary, 12 * ( salary + 200 ) 
FROM employees













SELECT * FROM employees; 
-- NULL Values 
SELECT 
first_name, 
last_name, 
salary, 
commission_pct 
FROM 
employees; 
SELECT 
first_name, 
salary, 
commission_pct, 
12 * salary * commission_pct 
FROM 
employees; 
-- Column Aliases 
SELECT first_name AS NAME, salary AS SALARY 
FROM employees; 
SELECT first_name "NAME", salary * 12 "ANNUAL SALARY" 
FROM employees


















-- Concatenation and Literal Character Strings
SELECT country_id || region_id AS "Country and Region" 
FROM countries; 
SELECT first_name || ' is a ' || job_id 
AS "Employee Details"
FROM employees; 
-- Eliminating Duplicate Rows 
SELECT job_id 
FROM employees; 
SELECT DISTINCT job_id 
FROM employees; 
SELECT DISTINCT(job_id) 
FROM employees; 
SELECT DISTINCT job_id, department_id 
FROM employees






-- WHERE Clause 
SELECT employee_id, first_name, job_id, department_id, 
salary 
FROM employees 
WHERE department_id = 100 ; 
SELECT employee_id, first_name, job_id, department_id, 
salary 
FROM employees 
WHERE last_name = 'King'; 
-- Comparison Conditions 
-- = > >= < <= <> 
SELECT first_name, salary 
FROM employees 
WHERE salary >= 5000; 
SELECT first_name, salary 
FROM employees 
WHERE salary <> 3000; 
SELECT first_name, salary 
FROM employees 
WHERE salary != 3000;







- BETWEEN ... AND 
SELECT first_name, salary 
FROM employees 
WHERE salary BETWEEN 1000 AND 5000; 
-- IN 
SELECT employee_id, first_name, salary, manager_id 
FROM employees 
WHERE manager_id BETWEEN 100 AND 103; 
SELECT employee_id, first_name, salary, manager_id 
FROM employees 
WHERE manager_id IN (100, 101, 102, 103); 
SELECT employee_id, first_name, salary, manager_id 
FROM employees 
WHERE LAST_NAME IN ('King', 'Ernst', 'Chen');


















- LIKE and WildCards 
SELECT first_name 
FROM employees 
WHERE first_name LIKE 'A%'; 
SELECT first_name 
FROM employees 
WHERE first_name LIKE '_a%'; 
SELECT first_name 
FROM employees 
WHERE first_name LIKE '%e'; 
SELECT employee_id, first_name, job_id 
FROM employees 
WHERE job_id LIKE '%ST\_%' ESCAPE '\'; 
-- IS NULL 
SELECT first_name, manager_id 
FROM employees 
WHERE manager_id IS NULL; 
SELECT first_name, job_id, commission_pct AS COMMISSION 
FROM employees 
WHERE commission_pct IS NULL;







Logical Operations 
SELECT employee_id, first_name, job_id, salary 
FROM employees 
WHERE salary >= 6000 
AND job_id LIKE 'SA%'; 
SELECT employee_id, first_name, job_id, salary 
FROM employees 
WHERE salary < 7000 
OR job_id LIKE 'ST%' 
SELECT employee_id, first_name, job_id, salary 
FROM employees 
WHERE job_id 
NOT IN ('ST_MAN', 'ST_CLERK', 'SA_MAN');





-- Sorting 
SELECT first_name, job_id, department_id, hire_date 
FROM employees 
ORDER BY first_name ; 
SELECT first_name, job_id, department_id, hire_date 
FROM employees 
ORDER BY first_name ASC; 
SELECT first_name, job_id, department_id, hire_date 
FROM employees 
ORDER BY first_name DESC; 
-- Sort by Column Alias 
SELECT employee_id, first_name, salary * 12 AS "Annual 
Salary" 
FROM employees 
ORDER BY "Annual Salary"; 
SELECT employee_id, first_name, salary * 12 AS "Annual 
Salary" 
FROM employees 
ORDER BY "Annual Salary" DESC; 
-- Sorting by multiple columns 
SELECT first_name, department_id, salary 
FROM employees 
ORDER BY first_name, job_id DESC; 
SELECT first_name, department_id, salary 
FROM employees 
ORDER BY first_name DESC, job_id DESC;









-- Working with Dates 
SELECT * 
FROM employees; 
SELECT * 
FROM employees 
WHERE HIRE_DATE = '03-JAN-06'; 
SELECT * 
FROM employees 
WHERE HIRE_DATE = '03-JAN-2006'; 
SELECT * 
FROM employees 
WHERE HIRE_DATE > '01-JAN-2001'; 
SELECT * 
FROM employees 
WHERE HIRE_DATE BETWEEN '01-JAN-2001' AND '01-JAN2010'; 
SELECT * 
FROM employees 
WHERE HIRE_DATE LIKE '%03';






















